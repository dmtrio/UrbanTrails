/* eslint-disable */
import Vue from 'vue'
import App from '../components/App.vue'

// Inspect the raw component options
test('App has a computed hook', () => {
  expect(App.mounted).toBeInstanceOf(Function)
})

